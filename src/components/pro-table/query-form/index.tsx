const QueryForm = defineComponent({
  name: 'QueryForm',
  setup() {
    const prefixCls = 'pro-table-query-form'

    return () => {
      return (
        <div class={prefixCls}>
          <h1>QueryForm</h1>
        </div>
      )
    }
  }
})

export default QueryForm
